# ออกแบบวิธีการทดสอบ Sensor เพื่อแสดงผลที่ Serial Monitor

## 1. วัตถุประสงค์
การทดสอบเซ็นเซอร์นี้มีวัตถุประสงค์เพื่อยืนยันว่าค่าเซ็นเซอร์สามารถอ่านได้ถูกต้องและแสดงผลผ่าน Serial Monitor อย่างชัดเจน โดยใช้ไมโครคอนโทรลเลอร์เป็นตัวรับข้อมูลจากเซ็นเซอร์และส่งค่าที่วัดได้ออกทางพอร์ต serial

## 2. อุปกรณ์ที่ใช้
- ไมโครคอนโทรลเลอร์ เช่น Arduino Uno
- เซ็นเซอร์ KY-028 หรือเซ็นเซอร์ชนิดอื่นที่ต้องการทดสอบ
- สายจอมินิและสายไฟ
- คอมพิวเตอร์ที่ใช้ Arduino IDE หรือ PlatformIO
- Serial Monitor สำหรับดูผลลัพธ์

## 3. หลักการทดสอบ
1. ต่อเซ็นเซอร์เข้ากับไมโครคอนโทรลเลอร์ตามขาทางไฟฟ้า
2. ตั้งค่า pin สำหรับรับข้อมูลจากเซ็นเซอร์
3. ใช้คำสั่งอ่านค่าเซ็นเซอร์จากตัวควบคุม
4. ส่งค่าออกทาง Serial Monitor ด้วยอัตราค่าคงที่ เช่น 9600 baud
5. ตรวจสอบว่าค่าที่แสดงมีความสอดคล้องกับสภาพแวดล้อมจริง

## 4. ขั้นตอนการออกแบบ
### 4.1 การต่อวงจร
- ต่อ VCC ของเซ็นเซอร์เข้ากับ 5V
- ต่อ GND เข้ากับ GND
- ต่อ Output ของเซ็นเซอร์เข้ากับขา analog หรือ digital ที่ต้องการอ่าน

### 4.2 การเขียนโปรแกรม
โปรแกรมจะทำงานโดย:
- เริ่มต้นการสื่อสาร Serial
- กำหนดขา input สำหรับเซ็นเซอร์
- อ่านค่าจากเซ็นเซอร์ในแต่ละรอบ
- แสดงค่าที่อ่านได้ลงบน Serial Monitor

### 4.3 ตัวอย่างโค้ด
```cpp
void setup() {
  Serial.begin(9600);
  pinMode(A0, INPUT);
}

void loop() {
  int sensorValue = analogRead(A0);
  Serial.print("Sensor Value: ");
  Serial.println(sensorValue);
  delay(1000);
}
```

## 5. วิธีการวิเคราะห์ผลลัพธ์
- หากค่าเซ็นเซอร์เปลี่ยนตามสภาพแวดล้อม แสดงว่าการทดสอบสำเร็จ
- หากค่าไม่เปลี่ยนแปลง อาจเกิดจากการต่อสายผิดหรือเซ็นเซอร์ชำรุด
- ควรทดสอบหลายครั้งและบันทึกผลเพื่อเปรียบเทียบ

## 6. ตัวอย่างผลลัพธ์ที่ควรเห็นใน Serial Monitor
```text
Sensor Value: 512
Sensor Value: 520
Sensor Value: 498
Sensor Value: 530
```

## 7. สรุป
การทดสอบเซ็นเซอร์เพื่อแสดงผลที่ Serial Monitor เป็นขั้นตอนพื้นฐานที่ช่วยให้เข้าใจการทำงานของเซ็นเซอร์ได้ดีขึ้น สามารถนำไปพัฒนาเป็นระบบตรวจวัดอัตโนมัติหรือระบบควบคุมต่าง ๆ ได้ในอนาคต
